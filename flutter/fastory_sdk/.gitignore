!lib/
.dart_tool/
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
.idea/
*.iml
.DS_Store
example/android/
example/ios/
example/.metadata
example/analysis_options.yaml
# The template's own test, written by `flutter create .` and targeting a MyApp the example does not
# have. The example's real suite (example/test/console_test.dart) is versioned and runs in CI.
example/test/widget_test.dart
