> Generated from packages/sdk/ios — never edited here. A Flutter plugin ships its native code as
> sources, so this copy is structural, not a stopgap awaiting an artifact dependency.

# fastory_sdk — iOS

- `Classes/*.swift`: verbatim copies of **every** source in the native Swift package
  (`packages/sdk/ios/Sources/FastorySDK`), generated by `packages/sdk/tools/sync_cores.py`. This used
  to be a hand-written list of filenames; it went stale twice as the core grew, so the rule is stated
  instead — the script globs the directory, and `sync_cores.py --check` is the CI guard.
- `Classes/FastorySdkPlugin.swift`: Flutter bridge (MethodChannel `fastory_sdk`, EventChannel
  `fastory_sdk/events`), plugin-specific — not part of the sync.

Any change to the core classes must be made in `packages/sdk/ios`, then propagated by running
`python3 packages/sdk/tools/sync_cores.py` and committing the result. Never edit the copies here.
